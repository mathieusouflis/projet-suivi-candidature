// Email.routes.js
// Ce fichier contient toutes les routes liées à la validation des adresses email

// TODO: Importer Express
// const express = require('express');
// const router = express.Router();

// TODO: Importer le controller des emails
// const EmailController = require('../controllers/EmailController');

// TODO: Routes pour la vérification et validation des emails
// - GET /check/:email : Vérifier si une adresse email est disponible
//   router.get('/check/:email', EmailController.checkEmailAvailability);
//
// - POST /validate : Valider le format d'une adresse email
//   router.post('/validate', EmailController.validateEmailFormat);
//
// - POST /verify/send : Envoyer un email de vérification
//   router.post('/verify/send', EmailController.sendVerificationEmail);
//
// - GET /verify/:token : Confirmer une adresse email via un token
//   router.get('/verify/:token', EmailController.confirmEmail);
//
// - GET /:email : Récupérer un utilisateur par son adresse email
//   router.get('/:email', EmailController.getUserByEmail);

// Exporter le routeur pour l'utiliser dans App.routes.js
// module.exports = router;